module piso(input [7:0]d,input clk,rst,output reg out);
reg [7:0]shift;
always@(posedge clk) begin
if(rst) begin
out<=0;
shift<=0;
end
else begin
shift<=d;
shift <= {1'b0, shift[7:1]};
end
end

endmodule
